# Using `FittingWorkRatios`

The command line interface is as
```
usage: java -jar FittingWorkRatios.jar -d <FILE> [-h] [-p] [-s] [-sv] [-vp]
    -d,--data-file <FILE>  specifies the data file
    -h,--help              print this help message
    -p,--partial           run partial running time simulation
    -s,--simple            run simple running time simulation
    -sv,--semi-verbose     run semi-verbose running time simulation
    -vp,--verbose-partial  run verbose-partial running time simulation
```

# Data file
A ready copy of the data file is available in [this Gist](https://gist.github.com/coderodde/4eccaef63d4034da3a9c0ea591d106ad#file-entropytoamortizedwork100-dat). 
Also, the link to the program that produced the above data file is in [IndexedLinkedListEntropy](https://github.com/coderodde/IndexedLinkedListEntropy) repository.
